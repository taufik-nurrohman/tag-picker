extends ../_index.pug

block state
  - const scripts = ['../index.min.js']
  - const styles = ['../index.min.css']
  - const title = 'Tag Picker'

block script
  script
    | const picker = new TagPicker(document.querySelector('input'));

block style
  style
    | .tag-picker {
    |   width: 100%;
    | }

block content
  main
    p: input(autofocus id='test' type='text' value='foo, bar, baz')
    p #[label(for='test' style={'text-decoration': 'underline'}) Click here] to focus to the tag picker!