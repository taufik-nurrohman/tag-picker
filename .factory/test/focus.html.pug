extends ../_index.pug

block state
  - const scripts = ['../index.js']
  - const styles = ['../index.min.css']
  - const title = 'Tag Picker'

block script
  script
    | const picker = new TagPicker(document.querySelector('input'));

block style
  style
    | .tag-picker {
    |   width: 100%;
    | }

block content
  main
    p: input(placeholder!='Tags go here&hellip;' type='text')